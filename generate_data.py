import sys; sys.path.append("./data_gen"); sys.path.append("./models"); import random; import pathlib; from schnapsen.game import SchnapsenGamePlayEngine; from schnapsen.bots import RdeepBot; from data_generation import MLDataBot; engine = SchnapsenGamePlayEngine(); rng = random.Random(42); base_bot = RdeepBot(num_samples=8, depth=4, rand=rng); replay_memory_dir = pathlib.Path("ML_replay_memories"); replay_memory_dir.mkdir(parents=True, exist_ok=True); replay_memory_file = replay_memory_dir / "replay_memory.txt"; data_bot = MLDataBot(base_bot, replay_memory_file); print("Generating training data..."); for i in range(100): engine.play_game(data_bot, base_bot, random.Random(i)); print("Data generation complete.")
